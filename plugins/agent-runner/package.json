{
	"name": "pi-agent-runner",
	"version": "0.1.0",
	"description": "Shared subprocess agent runner for pi extensions: spawn one pi child in JSON mode, fold its event stream, and bound it.",
	"type": "module",
	"main": "./src/index.ts",
	"exports": { ".": "./src/index.ts" },
	"files": ["src", "README.md"],
	"scripts": { "test": "bun test", "typecheck": "tsc --noEmit" },
	"keywords": ["pi", "pi-package", "agent", "subagent", "runner"],
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^22.0.0",
		"bun-types": "^1.4.2",
		"typescript": "^5.6.0"
	}
}
